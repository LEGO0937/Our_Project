

uiShader
ㄴ> 인스턴싱 버퍼에는 각 오브젝트의 변환행렬 -> 화면위치 조절
2. 숫자는? -> 매번 셰이더 업데이트에서 받아오는 elapsed time으로 카운터를 올리고 각 자릿수마다.
분 초 밀리초

Shader종류 Shader들은 모두 인스턴싱을 위한 셰이더임

BillBoardShader 이미지명, 로드데이터명을 받고 빌보드들을 생성함 
ModelShader bin파일명,로드데이터명을 받고 오브젝트들을 생성함
SkinedShader bin파일명, 로드 데이터명을 받고 스킨오브젝트들을 생성함

ㄴ> 여기서 셰이더마다 각 부위에 질량과 충돌체크결과 적용여부의 값을 넣어주려면?
ㄴ> 파생 셰이더로 더 추가해서 buildobject를 따로 넣는수밖에..
ㄴ> buildoBject에서 셰이더 자체의 멤버인 충돌체크를 하는 오브젝트들을 담고있다고
알릴 수 있는 변수에 데이터 입력


질량이 넣어졌다고 가정, 충돌여부도 들어갔다고가정.
그럼 
씬내에서는 충돌처리를 어떻게 수행할 것인가? 
충돌처리를 적용하는 셰이더들을 담는 리스트를 하나 더 만들자....
어차피 만드는 시점에서 이게 충돌처리에 사용되는지는 바로 알 수있기 때문에

어떤 종류의 오브젝트가 특정 오브젝트와만의 충돌반응은 어떻게?
RaceTurn셰이더에 플레이어가 현재 얼만큼 돌았는지는 어떻게 확인할 것인지?.

파티클은 어떻게 관리할 것인지?




ui셰이더
GaugeShader 게이지 전용
TimeCounter : 경과시간 전용
RaceTurn : 트랙 돈 횟수

애니메이션 
up  -> isWalking =true -> 현 idle 버전을 run버전으로 trans

down -> isWalking =true 위와 일치

down 